import PlaceholderPage from '../layout/PlaceholderPage'

function SongLibraryPage(): React.JSX.Element {
  return (
    <PlaceholderPage
      icon="📚"
      title="Song Library"
      description="Saved songs will appear here once imported songs can be stored between sessions."
    />
  )
}

export default SongLibraryPage
